<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <activity
            android:name=".features.activity.presenter.AuthActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:launchMode="singleTop">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="refactoringlife.com"
                    android:pathPrefix="/auth"
                    android:scheme="huella" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="refactoringlife.com"
                    android:pathPrefix="/auth/reset-password"
                    android:scheme="https" />
            </intent-filter>

        </activity>
    </application>
</manifest>

<!--    android:host="delightful-pixie-f83dc1.netlify.app"    <- url de prueba web ficticia-->
<!--    android:host="refactoringlife.com"                    <- url del mock  -->